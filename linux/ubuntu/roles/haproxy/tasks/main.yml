---
- name: install haproxy
  apt:
    name: haproxy
    state: present
- name: copy haproxy cert
  copy:
    dest: /var/lib/jenkins/
    src: files/jenkins.pem
- name: copy haproxy config
  copy:
    dest: /etc/haproxy/haproxy.cfg
    src: files/jenkins-haproxy.cfg
    force: true
- name: restart haproxy service
  systemd:
    state: restarted
    name: haproxy
